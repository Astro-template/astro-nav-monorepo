{
  "name": "@astro-nav/worker",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 execute astro-nav-db --local --file=./migrations/0001_init.sql"
  },
  "dependencies": {
    "mustache": "^4.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "@types/mustache": "^4.2.6",
    "typescript": "^5.9.3",
    "wrangler": "^4.114.0"
  }
}
